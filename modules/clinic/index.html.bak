<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <title>لوحة الطبيب - Doctor.ai</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" />
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700;800&display=swap" rel="stylesheet" />
    <link rel="stylesheet" href="../../CSS/style.css" />
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { font-family: 'Cairo', sans-serif; background: #f2f4f8; direction: rtl; padding: 16px; padding-bottom: 80px; min-height: 100vh; }
        .app-container { max-width: 480px; margin: 0 auto; }

        .doctor-header { background: #fff; padding: 16px; border-radius: 20px; box-shadow: 0 2px 12px rgba(0,0,0,0.04); margin-bottom: 16px; }
        .header-top { display: flex; justify-content: space-between; align-items: center; }
        .user-info { display: flex; align-items: center; gap: 12px; }
        .avatar { width: 48px; height: 48px; border-radius: 50%; background: var(--gateway-doctor); color: #fff; display: flex; align-items: center; justify-content: center; font-size: 20px; font-weight: 700; }
        .user-details h3 { font-size: 16px; color: #1e293b; font-weight: 700; }
        .user-details .sub { font-size: 12px; color: #94a3b8; }
        .header-actions { display: flex; gap: 10px; }
        .btn-notification { background: #f1f5f9; border: none; width: 40px; height: 40px; border-radius: 50%; font-size: 18px; color: #1e293b; cursor: pointer; position: relative; }
        .btn-notification .badge { position: absolute; top: -2px; right: -2px; background: #ef4444; color: #fff; font-size: 10px; width: 18px; height: 18px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-weight: 700; }
        .btn-logout { background: transparent; border: none; font-size: 20px; color: #94a3b8; cursor: pointer; padding: 8px; border-radius: 50%; transition: 0.2s; }
        .btn-logout:active { background: #f1f5f9; }

        .stats-grid { display: grid; grid-template-columns: repeat(4, 1fr); gap: 10px; margin-bottom: 16px; }
        .stat-card { background: #fff; padding: 14px 8px; border-radius: 16px; text-align: center; box-shadow: 0 1px 4px rgba(0,0,0,0.04); }
        .stat-card .num { font-size: 22px; font-weight: 800; color: var(--gateway-doctor); }
        .stat-card .label { font-size: 11px; color: #94a3b8; }

        .services-hub-btn { display: block; background: linear-gradient(135deg, var(--gateway-doctor) 0%, #1e3a5f 100%); color: #fff; padding: 16px; border-radius: 16px; text-decoration: none; text-align: center; margin-bottom: 16px; box-shadow: 0 4px 20px rgba(26,86,219,0.25); transition: 0.3s; }
        .services-hub-btn:active { transform: scale(0.98); }
        .services-hub-btn i { font-size: 24px; display: block; margin-bottom: 4px; }
        .services-hub-btn span { font-size: 18px; font-weight: 700; }
        .services-hub-btn small { font-size: 13px; opacity: 0.85; display: block; margin-top: 2px; }

        .patients-section { background: #fff; border-radius: 16px; padding: 16px; box-shadow: 0 1px 4px rgba(0,0,0,0.04); margin-bottom: 16px; }
        .patients-section .title { font-size: 15px; font-weight: 700; color: #1e293b; margin-bottom: 12px; display: flex; align-items: center; gap: 8px; }
        .patients-section .title i { color: var(--gateway-doctor); }
        .patient-item { display: flex; justify-content: space-between; align-items: center; padding: 10px 0; border-bottom: 1px solid #f3f4f6; }
        .patient-item:last-child { border-bottom: none; }
        .patient-info .name { font-weight: 600; color: #1e293b; font-size: 14px; }
        .patient-info .detail { font-size: 12px; color: #64748b; display: block; }
        .patient-status { font-size: 11px; padding: 2px 12px; border-radius: 12px; font-weight: 600; }
        .patient-status.today { background: #dbeafe; color: #1e40af; }
        .patient-status.week { background: #fef3c7; color: #92400e; }

        .quick-services { display: grid; grid-template-columns: repeat(4, 1fr); gap: 8px; margin-bottom: 16px; }
        .quick-service { background: #fff; padding: 10px 4px; border-radius: 12px; text-align: center; text-decoration: none; color: #1e293b; box-shadow: 0 1px 4px rgba(0,0,0,0.04); transition: 0.2s; border: 1px solid transparent; }
        .quick-service:active { transform: scale(0.95); background: #f8fafc; border-color: var(--gateway-doctor); }
        .quick-service i { font-size: 20px; color: var(--gateway-doctor); display: block; margin-bottom: 2px; }
        .quick-service span { font-size: 10px; font-weight: 600; }

        .bottom-nav { position: fixed; bottom: 0; left: 0; right: 0; background: #fff; display: flex; justify-content: space-around; align-items: center; padding: 6px 0 10px; box-shadow: 0 -4px 20px rgba(0,0,0,0.04); z-index: 100; border-top: 1px solid #f1f5f9; max-width: 480px; margin: 0 auto; }
        .nav-item { display: flex; flex-direction: column; align-items: center; cursor: pointer; color: #94a3b8; transition: 0.3s; padding: 4px 12px; border: none; background: transparent; font-family: inherit; }
        .nav-item i { font-size: 20px; }
        .nav-item span { font-size: 10px; margin-top: 2px; }
        .nav-item.active { color: var(--gateway-doctor); }
        .nav-item.active i { transform: scale(1.1); }
        .nav-item:active { transform: scale(0.92); }

        @media (max-width: 480px) { .stats-grid { grid-template-columns: repeat(2, 1fr); } .quick-services { grid-template-columns: repeat(3, 1fr); } }
        @media (min-width: 481px) { .bottom-nav { border-left: 1px solid #e5e7eb; border-right: 1px solid #e5e7eb; } }
    </style>
</head>
<body>
    <div class="app-container">
        <header class="doctor-header">
            <div class="header-top">
                <div class="user-info">
                    <div class="avatar" id="userAvatar">ط</div>
                    <div class="user-details">
                        <h3 id="userName">د. <span style="color:var(--gateway-doctor);">أحمد سامي</span></h3>
                        <span class="sub" id="userId"><i class="fas fa-id-card"></i> #DR-2024</span>
                    </div>
                </div>
                <div class="header-actions">
                    <button class="btn-notification" onclick="alert('📬 لديك 3 إشعارات جديدة')">
                        <i class="fas fa-bell"></i>
                        <span class="badge">3</span>
                    </button>
                    <button class="btn-logout" onclick="logout()"><i class="fas fa-sign-out-alt"></i></button>
                </div>
            </div>
        </header>

        <div class="stats-grid">
            <div class="stat-card"><div class="num" id="patientsToday">8</div><div class="label">مرضى اليوم</div></div>
            <div class="stat-card"><div class="num" id="patientsWeek">24</div><div class="label">مرضى هذا الأسبوع</div></div>
            <div class="stat-card"><div class="num" id="appointmentsCount">5</div><div class="label">مواعيد قادمة</div></div>
            <div class="stat-card"><div class="num" id="prescriptionsCount">12</div><div class="label">روشتات</div></div>
        </div>

        <a href="doctor-services-app.html" class="services-hub-btn">
            <i class="fas fa-th-large"></i>
            <span>🩺 البوابات الذكية</span>
            <small>5 أقسام (خدمات الطبيب)</small>
        </a>

        <div class="quick-services">
            <a href="services/doctor-appointments.html" class="quick-service">
                <i class="fas fa-calendar-check"></i>
                <span>المواعيد</span>
            </a>
            <a href="services/doctor-patients.html" class="quick-service">
                <i class="fas fa-users"></i>
                <span>المرضى</span>
            </a>
            <a href="services/doctor-prescriptions.html" class="quick-service">
                <i class="fas fa-prescription"></i>
                <span>الروشتات</span>
            </a>
            <a href="services/doctor-reports.html" class="quick-service">
                <i class="fas fa-chart-line"></i>
                <span>التقارير</span>
            </a>
        </div>

        <div class="patients-section">
            <div class="title"><i class="fas fa-user-md"></i> آخر المرضى</div>
            <div id="recentPatients">
                <div class="patient-item">
                    <div class="patient-info">
                        <span class="name">محمد علي</span>
                        <span class="detail">🩺 ضغط الدم - 15/07/2026</span>
                    </div>
                    <span class="patient-status today">اليوم</span>
                </div>
                <div class="patient-item">
                    <div class="patient-info">
                        <span class="name">سارة حسن</span>
                        <span class="detail">🩺 السكر - 14/07/2026</span>
                    </div>
                    <span class="patient-status week">هذا الأسبوع</span>
                </div>
                <div class="patient-item">
                    <div class="patient-info">
                        <span class="name">خالد محمود</span>
                        <span class="detail">🩺 الغدة الدرقية - 12/07/2026</span>
                    </div>
                    <span class="patient-status week">هذا الأسبوع</span>
                </div>
            </div>
        </div>

        <nav class="bottom-nav">
            <div class="nav-item active" onclick="navigateTo('home')">
                <i class="fas fa-home"></i>
                <span>الرئيسية</span>
            </div>
            <div class="nav-item" onclick="navigateTo('patients')">
                <i class="fas fa-users"></i>
                <span>المرضى</span>
            </div>
            <div class="nav-item" onclick="navigateTo('prescriptions')">
                <i class="fas fa-prescription"></i>
                <span>الروشتات</span>
            </div>
            <div class="nav-item" onclick="navigateTo('appointments')">
                <i class="fas fa-calendar"></i>
                <span>المواعيد</span>
            </div>
            <div class="nav-item" onclick="navigateTo('more')">
                <i class="fas fa-th-large"></i>
                <span>البوابات</span>
            </div>
        </nav>
    </div>

    <script>
        // ============================================================
        // ===== التعديل الأساسي: جلب البيانات من السيرفر =====
        // ============================================================

        const API_BASE = 'http://localhost:5000/api';

        // ----- جلب التوكن -----
        function getToken() {
            return localStorage.getItem('token');
        }

        // ----- جلب بيانات الطبيب من السيرفر -----
        async function fetchDoctorData() {
            const token = getToken();
            if (!token) {
                window.location.href = '../../auth/login.html';
                return;
            }

            try {
                const res = await fetch(`${API_BASE}/auth/me`, {
                    headers: {
                        'Authorization': `Bearer ${token}`,
                        'Content-Type': 'application/json'
                    }
                });

                if (!res.ok) {
                    if (res.status === 401 || res.status === 403) {
                        logout();
                    }
                    throw new Error(`HTTP ${res.status}`);
                }

                const data = await res.json();
                const user = data.user;
                const profile = data.profile;

                // تحديث الواجهة باسم الطبيب والعيادة
                const doctorName = user.full_name || 'الطبيب';
                const clinicName = profile?.clinic_name || '';
                const specialty = profile?.specialty || '';
                const licenseNumber = profile?.license_number || user.id || 'DR-2024';

                document.getElementById('userName').innerHTML = 'د. <span style="color:var(--gateway-doctor);">' + doctorName.replace('د. ', '') + '</span>';
                document.getElementById('userId').innerHTML = '<i class="fas fa-id-card"></i> ' + licenseNumber;
                document.getElementById('userAvatar').textContent = doctorName.charAt(0) || 'ط';

                // جلب الإحصائيات الحقيقية بعد جلب بيانات المستخدم
                await loadDoctorStats();

                console.log('✅ تم جلب بيانات الطبيب:', user, profile);
                return data;
            } catch (err) {
                console.error('❌ فشل جلب بيانات الطبيب:', err);
                const name = localStorage.getItem('doctor_name') || 'الطبيب';
                document.getElementById('userName').innerHTML = 'د. <span style="color:var(--gateway-doctor);">' + name.replace('د. ', '') + '</span>';
                document.getElementById('userAvatar').textContent = name.charAt(0) || 'ط';
            }
        }

        // ============================================================
        // ===== جلب الإحصائيات الحقيقية للطبيب =====
        // ============================================================
        async function loadDoctorStats() {
            const token = getToken();
            if (!token) return;

            try {
                const res = await fetch(`${API_BASE}/stats`, {
                    headers: {
                        'Authorization': `Bearer ${token}`,
                        'Content-Type': 'application/json'
                    }
                });

                if (!res.ok) {
                    console.warn('⚠️ فشل جلب إحصائيات الطبيب:', res.status);
                    return;
                }

                const data = await res.json();
                if (data.success) {
                    const s = data.stats;
                    document.getElementById('patientsToday').textContent = s.patientsToday || 0;
                    document.getElementById('patientsWeek').textContent = s.patientsWeek || 0;
                    document.getElementById('appointmentsCount').textContent = s.appointments || 0;
                    document.getElementById('prescriptionsCount').textContent = s.prescriptions || 0;
                }
            } catch (err) {
                console.error('❌ فشل جلب إحصائيات الطبيب:', err);
            }
        }

        // ----- تسجيل الخروج (معدل) -----
        function logout() {
            if (confirm('هل أنت متأكد من الخروج من حسابك؟')) {
                localStorage.clear();
                sessionStorage.clear();
                window.location.href = '../../auth/login.html';
            }
        }

        // ----- التنقل في الشريط السفلي (كما هو) -----
        function navigateTo(page) {
            document.querySelectorAll('.nav-item').forEach(item => item.classList.remove('active'));
            const navItems = document.querySelectorAll('.nav-item');
            const pages = ['home', 'patients', 'prescriptions', 'appointments', 'more'];
            const index = pages.indexOf(page);
            if (index !== -1) navItems[index].classList.add('active');
            const pageMap = {
                'home': 'index.html',
                'patients': 'services/doctor-patients.html',
                'prescriptions': 'services/doctor-prescriptions.html',
                'appointments': 'services/doctor-appointments.html',
                'more': 'doctor-services-app.html'
            };
            if (pageMap[page]) { window.location.href = pageMap[page]; }
        }

        // ----- تحميل الصفحة -----
        document.addEventListener('DOMContentLoaded', async function() {
            console.log('🚀 جاري تحميل لوحة الطبيب...');
            await fetchDoctorData();
            console.log('✅ لوحة الطبيب (مربوطة بالسيرفر) جاهزة!');
        });

        // ربط الدوال بالـ window للاستخدام في الـ HTML
        window.logout = logout;
        window.navigateTo = navigateTo;
    </script>
</body>
</html>